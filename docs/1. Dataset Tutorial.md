---
title: 1. Dataset Tutorial
nav_order: 2
has_children: true
has_toc: false
---
# 1. Dataset Tutorial

## Overview

This section describes how the dataset used in this project is constructed, organized, and prepared for machine learning tasks. The dataset consists of both real and AI-generated images and is designed to support a reproducible pipeline for feature extraction and classification.

The workflow includes dataset acquisition, preprocessing, metadata construction, and the creation of balanced training and test splits. All stages are metadata-driven, ensuring consistent data handling and preventing leakage between datasets.

<p align="center">
  <img src="images/dataset-tutorial-pipeline.png" width="500">
</p>

<p align="center"><em>Figure: Metadata-driven dataset preparation pipeline.</em></p>

The dataset is composed of multiple image sources representing both real and AI-generated content. Real images are drawn from established datasets, while AI-generated images are obtained from modern generative models.

The real-image portion of the dataset includes ImageNet_1K_256, MS_COCO_2017, and OpenImages, while AI-generated images are sourced from DiffusionDB, SDXL_Generated_10K, and MidJourney. This combination provides a balanced and diverse set of images for classification.

Each image is associated with metadata describing the filename, class label (`real` or `ai`), and source dataset. This design allows all processing steps to operate independently of directory structure, ensuring flexibility and consistency across the pipeline.

Images are stored in two forms: raw images in their original format, and preprocessed images that have been converted to grayscale and resized to 256×256 pixels. The preprocessed representation is used for all feature extraction steps, providing a consistent input format across the pipeline.

The dataset is divided into training and test subsets using metadata-based splitting. The resulting datasets, `train_metadata.csv` and `test_metadata.csv`, are constructed to be balanced across both dataset sources and class labels, with a fixed random seed ensuring reproducibility.

All dataset partitioning is defined at the metadata level, ensuring that images are never duplicated or moved between subsets. This approach prevents data leakage and preserves the integrity of model evaluation.

For a complete overview of how files are organized, see the [Project Directory Structure](Project_Directory_Structure.md).

## Workflow

This section prepares the dataset through a sequence of steps, beginning with dataset construction and ending with the creation of training and test splits.

- [01 Build Dataset](01_Build_Dataset.md) — construct dataset from source images
- [02 Preprocess Images](02_Preprocess_Images.md) — standardize image format
- [03 Combine and Split](03_Combine_and_Split.md) — create balanced train/test splits

Each stage is implemented as a separate notebook and produces artifacts used in subsequent steps of the pipeline.

## Notes

- Dataset construction is optional since prebuilt datasets are already available for usage
- All processing is metadata-driven to ensure reproducibility and consistency
- Preprocessed datasets are the default input for feature extraction
- Raw datasets are retained primarily for dataset construction and experimentation
- Metadata-based splitting ensures that images are not shared between training and test sets, preventing data leakage during model evaluation

